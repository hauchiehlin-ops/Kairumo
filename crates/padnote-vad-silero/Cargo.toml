[package]
name = "padnote-vad-silero"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
# 內部 crate，不發佈到 crates.io（workspace 內以 path 互相引用）。
publish = false

[dependencies]
padnote-asr = { path = "../padnote-asr" }
ort = { workspace = true }

[lints]
workspace = true
